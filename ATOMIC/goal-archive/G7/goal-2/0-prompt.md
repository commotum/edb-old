# Goal 2 — Continuation prompt

```text
Execute the block-backed database-values child using
/home/jake/Developer/atomic/goal-2/0-plan.md and
/home/jake/Developer/atomic/goal-2/0-loop.md.

This is Stage 2 of /home/jake/Developer/atomic/goal-0/0-plan.md. Build selective
authenticated log/index/metadata structures and immutable block-backed snapshots
over the verified opaque PostgreSQL store. Reuse existing Rust tree cursors,
recent-tier merging, query/Pull/navigation and speculative value semantics. Use
local datomic_pro_docs as semantic authority and 1.0.7705 as architectural evidence.

Preserve current capabilities and current-version correctness. No eager database
fallback, legacy SQL catalog facade, old-format readers, JVM parity, other backend
project or recursive scaffolds. Reconcile and finish existing work, verify actual
PostgreSQL/application-library paths with measured complete costs, update child and
parent, and return to Goal 0 to execute the remaining cutover stages.
```
