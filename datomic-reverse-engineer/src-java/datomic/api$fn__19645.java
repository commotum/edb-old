/*
 * Decompiled with CFR 0.152.
 * 
 * Could not load the following classes:
 *  clojure.lang.AFunction
 *  clojure.lang.IFn
 *  clojure.lang.Indexed
 *  clojure.lang.Keyword
 *  clojure.lang.RT
 *  clojure.lang.Var
 */
package datomic;

import clojure.lang.AFunction;
import clojure.lang.IFn;
import clojure.lang.Indexed;
import clojure.lang.Keyword;
import clojure.lang.RT;
import clojure.lang.Var;

public final class api$fn__19645
extends AFunction {
    public static final Var const__0 = RT.var((String)"clojure.core", (String)"seq");
    public static final Var const__1 = RT.var((String)"datomic.api", (String)"administer-system");
    public static final Var const__2 = RT.var((String)"datomic.api", (String)"since");
    public static final Var const__3 = RT.var((String)"datomic.api", (String)"seek-datoms");
    public static final Var const__4 = RT.var((String)"datomic.api", (String)"t->tx");
    public static final Var const__5 = RT.var((String)"datomic.api", (String)"db");
    public static final Var const__6 = RT.var((String)"datomic.api", (String)"part");
    public static final Var const__7 = RT.var((String)"datomic.api", (String)"invoke");
    public static final Var const__8 = RT.var((String)"datomic.api", (String)"history");
    public static final Var const__9 = RT.var((String)"datomic.api", (String)"remove-tx-report-queue");
    public static final Var const__10 = RT.var((String)"datomic.api", (String)"as-of-t");
    public static final Var const__11 = RT.var((String)"datomic.api", (String)"entid");
    public static final Var const__12 = RT.var((String)"datomic.api", (String)"create-database");
    public static final Var const__13 = RT.var((String)"datomic.api", (String)"cancel");
    public static final Var const__14 = RT.var((String)"datomic.api", (String)"entity");
    public static final Var const__15 = RT.var((String)"datomic.api", (String)"pull-many");
    public static final Var const__16 = RT.var((String)"datomic.api", (String)"sync-index");
    public static final Var const__17 = RT.var((String)"datomic.api", (String)"as-of");
    public static final Var const__18 = RT.var((String)"datomic.api", (String)"qseq");
    public static final Var const__19 = RT.var((String)"datomic.api", (String)"squuid");
    public static final Var const__20 = RT.var((String)"datomic.api", (String)"tempid");
    public static final Var const__21 = RT.var((String)"datomic.api", (String)"q");
    public static final Var const__22 = RT.var((String)"datomic.api", (String)"transact");
    public static final Var const__23 = RT.var((String)"datomic.api", (String)"transact-async");
    public static final Var const__24 = RT.var((String)"datomic.api", (String)"sync-excise");
    public static final Var const__25 = RT.var((String)"datomic.api", (String)"sync-schema");
    public static final Var const__26 = RT.var((String)"datomic.api", (String)"is-filtered");
    public static final Var const__27 = RT.var((String)"datomic.api", (String)"release");
    public static final Var const__28 = RT.var((String)"datomic.api", (String)"entid-at");
    public static final Var const__29 = RT.var((String)"datomic.api", (String)"next-t");
    public static final Var const__30 = RT.var((String)"datomic.api", (String)"attribute");
    public static final Var const__31 = RT.var((String)"datomic.api", (String)"db-stats");
    public static final Var const__32 = RT.var((String)"datomic.api", (String)"sync");
    public static final Var const__33 = RT.var((String)"datomic.api", (String)"add-listener");
    public static final Var const__34 = RT.var((String)"datomic.api", (String)"query");
    public static final Var const__35 = RT.var((String)"datomic.api", (String)"tx-report-queue");
    public static final Var const__36 = RT.var((String)"datomic.api", (String)"get-database-names");
    public static final Var const__37 = RT.var((String)"datomic.api", (String)"connect");
    public static final Var const__38 = RT.var((String)"datomic.api", (String)"with");
    public static final Var const__39 = RT.var((String)"datomic.api", (String)"since-t");
    public static final Var const__40 = RT.var((String)"datomic.api", (String)"shutdown");
    public static final Var const__41 = RT.var((String)"datomic.api", (String)"delete-database");
    public static final Var const__42 = RT.var((String)"datomic.api", (String)"implicit-part");
    public static final Var const__43 = RT.var((String)"datomic.api", (String)"rename-database");
    public static final Var const__44 = RT.var((String)"datomic.api", (String)"basis-t");
    public static final Var const__45 = RT.var((String)"datomic.api", (String)"ident");
    public static final Var const__46 = RT.var((String)"datomic.api", (String)"datoms");
    public static final Var const__47 = RT.var((String)"datomic.api", (String)"log");
    public static final Var const__48 = RT.var((String)"datomic.api", (String)"squuid-time-millis");
    public static final Var const__49 = RT.var((String)"datomic.api", (String)"resolve-tempid");
    public static final Var const__50 = RT.var((String)"datomic.api", (String)"tx-range");
    public static final Var const__51 = RT.var((String)"datomic.api", (String)"tx->t");
    public static final Var const__52 = RT.var((String)"datomic.api", (String)"index-range");
    public static final Var const__53 = RT.var((String)"datomic.api", (String)"function");
    public static final Var const__54 = RT.var((String)"datomic.api", (String)"index-pull");
    public static final Var const__55 = RT.var((String)"datomic.api", (String)"entity-db");
    public static final Var const__56 = RT.var((String)"datomic.api", (String)"touch");
    public static final Var const__57 = RT.var((String)"datomic.api", (String)"filter");
    public static final Var const__58 = RT.var((String)"datomic.api", (String)"pull");
    public static final Var const__59 = RT.var((String)"datomic.api", (String)"implicit-part-id");
    public static final Var const__60 = RT.var((String)"datomic.api", (String)"gc-storage");
    public static final Var const__61 = RT.var((String)"datomic.api", (String)"request-index");
    public static final Var const__66 = RT.var((String)"clojure.core", (String)"alter-meta!");
    public static final Var const__67 = RT.var((String)"clojure.core", (String)"assoc");
    public static final Keyword const__68 = RT.keyword(null, (String)"doc");
    public static final Var const__70 = RT.var((String)"clojure.core", (String)"chunked-seq?");
    public static final Var const__71 = RT.var((String)"clojure.core", (String)"chunk-first");
    public static final Var const__72 = RT.var((String)"clojure.core", (String)"chunk-rest");
    public static final Var const__75 = RT.var((String)"clojure.core", (String)"first");
    public static final Var const__76 = RT.var((String)"clojure.core", (String)"next");

    public static Object invokeStatic() {
        Object seq_19641 = ((IFn)const__0.getRawRoot()).invoke((Object)RT.map((Object[])new Object[]{const__1, "Administer system. Takes an options map with a required :action key.\n   Throws on failure. Actions include:\n   \n   Release Object Cache\n   :action      :release-object-cache  \n\n   Effect: Clear all entries from the Object Cache.\n\n   Upgrade Schema\n   :action      :upgrade-schema\n   :uri         a URI as per connect\n\n   Effect: Upgrades the base schema of a database to the latest version.\n   NOTE: Read https://docs.datomic.com/operation/deployment.html before calling.", const__2, "Returns the value of the database since some point t, exclusive\n   t can be a transaction number, transaction ID, or Date.", const__3, "Raw access to the index data, by index. The index must be supplied,\n\t and, optionally, one or more leading components of the index can be supplied for the initial search.\n\t Note that, unlike the datoms function, there need not be an exact match on the supplied components.\n\t The iteration will begin at or after the point in the index where the components would reside.\n\t Further, the iteration is not bound by the supplied components, and will only terminate\n\t at the end of the index. Thus you will have to supply your own termination logic, as you rarely\n\t want the entire index. As such, seek-datoms is for more advanced applications, and datoms should be preferred\n\t wherever it is adequate. See also - entid-at.\n\n         :eavt and :aevt indexes will contain all datoms\n         :avet contains datoms for attributes where :db/index = true.\n         :vaet contains datoms for attributes of :db.type/ref\n         :vaet is the reverse index\n\n         See datoms for a description of the returned value.", const__4, "Return the transaction id associated with a t value.", const__5, "Retrieves a value of the database for reading. Does not\n  communicate with the transactor, nor block.", const__6, "Return the partition associated with an entity id.", const__7, "Lookup the database function named by eid-or-ident, and call it with args.", const__8, "Returns a special database containing all assertions and\nretractions across time. This special database can be used for\ndatoms and index-range calls and queries, but not for entity or\nwith calls. as-of and since bounds are also supported. Note that\nqueries will get all of the additions and retractions, which can be\ndistinguished by the fifth datom field :added (true for add/assert)\n[e a v tx added]", const__9, "Removes the queue associated with this connection.", const__10, "Returns the as-of point, or nil if none", const__11, "Returns the entity id associated with a symbolic keyword, or the id\n   itself if passed.", const__12, "Creates database specified by uri. Returns true if the\n   database was created, false if it already exists. See connect\n   for a description of the URI syntax.", const__13, "Cancels the current Datomic operation (query or transaction).\n\nThrows an ex-info with an anomaly to the original caller.\n\nanomaly-map is an anomaly as described by https://github.com/cognitect-labs/anomalies.\n\n:cognitect.anomalies/category is a required key, valid values are:\n\n  :cognitect.anomalies/incorrect\n  :cognitect.anomalies/conflict\n\nWhen :cognitect.anomalies/message is provided, the message will be used as the Exception's detail message\n\nOther keys should be namespace-qualified.\n\nAll data passed to cancel must be fressian-serializable.", const__14, "Returns a dynamic map of the entity's attributes for the given id, ident or lookup ref.\n   Entities implement\n     clojure.lang.Associative\n     clojure.lang.ILookup\n     clojure.lang.IPersistentCollection\n     clojure.lang.Seqable\n     datomic.Entity", const__15, "Returns hierarchical selections of attributes for eids. \n    See https://docs.datomic.com/query/query-pull.html for more information.\n\n    You can get information about the I/O reads performed by a\n    pull with io-stats. To request io-stats, pass :io-context\n    (a qualified keyword) as an option to pull-many. The return\n    value will be a map with\n\n    :ret                the result of the pull\n    :io-stats           io-stats for the pull\n\n    The io-stats map includes:\n\n    :io-context         the io-context passed in\n    :api                :tx-with\n    :api-ms             msec to peform the API call\n    :reads              breakout of reads by cache tier and index sort\n    :nested             breakout iff nested queries set :io-context\n\n    See https://docs.datomic.com/reference/io-stats.html.", const__16, "Used to coordinate with background indexing jobs. Returns a\n    future that will acquire a database value that is indexed\n    through time <= t.\n\n    Does not communicate with the transactor, so the future may be\n    available immediately.\n\n    The future can take arbitrarily long to complete.  Waiters\n    should specify a timeout.", const__17, "Returns the value of the database as of some point t, inclusive.\n   t can be a transaction number, transaction ID, or Date.", const__18, "Performs the query described by query-map (as per 'query'),\n         returning a lazy seq on the results.  Item transformations such as\n         'pull' are deferred until the seq is consumed. For queries with\n         pull(s), this results in:\n\n         * reduced memory use and the ability to execute larger queries\n         * lower latency before the first results are returned\n\n         The returned seq object efficiently supports 'count'.", const__19, "Constructs a semi-sequential UUID. Useful for creating UUIDs\nthat don't fragment indexes. Returns a UUID whose most significant\n32 bits are currentTimeMillis rounded to seconds.", const__20, "Generate a tempid in the specified partition. Within the scope\n   of a single transaction, tempids map consistently to permanent\n   ids. Values of n from -1 to -1000000, inclusive, are reserved for\n   user-created tempids.", const__21, "Executes a query against inputs.\n\n   Inputs are data sources e.g. a database value retrieved from\n   Connection.db, a list of lists, and/or rules. If only one data\n   source is provided, no :in section is required, else the :in\n   section describes the inputs.\n\n   query can be a map, list, or string:\n\n   The query map form is {:find vars-and-aggregates\n                          :with vars-included-but-not-returned\n                          :in sources\n                          :where clauses}\n   where vars, sources and clauses are lists.\n\n  :with is optional, and names vars to be kept in the aggregation set but\n   not returned.\n\n   The query list form is [:find ?var1 ?var2 ...\n                           :with ?var3 ...\n                           :in $src1 $src2 ...\n                           :where clause1 clause2 ...]\n   The query list form is converted into the map form internally.\n\n   The query string form is a string which, when read, results\n   in a query list form or query map form.\n\n   Query parse results are cached.\n\n   Returns a data structure based on the find specification passed in.\n   See https://docs.datomic.com/query/query-data-reference.html#find-specs.", const__22, "Given a connection and a set of information (tx-data), submits\n         a transaction, blocking until a result is available. d/transact\n         updates the connection's shared reference to the value of the\n         database by swapping in the result of d/with.\n\n         Returns a completed future. See d/with for a description of\n         tx-data and the return map that will be placed in the future.\n\n         An exception indicates either that a transaction failed, or\n         that the result of the transaction is not known after a\n         communication failure. For detailed information on programmatic\n         error handling, see https://docs.datomic.com/api/error-handling.html.\n         Note that an exception may occur either when invoking the API\n         or when dereferencing the returned reference.\n\n         Optional args:\n          :hints opaque value, used to optimize transaction performance.\n          See d/with or https://docs.datomic.com/reference/hints.html.\n\n          :io-context a qualified keyword\n          When provided, the return map will have an additional :io-stats key describing\n          information about the I/O reads performed by the transaction, if the connection\n          supports io-stats.\n          See https://docs.datomic.com/reference/io-stats.html.", const__23, "Same as transact, but returns its future immediately.", const__24, "Used to coordinate with background excision. Returns a\n    future that will acquire a database value that is aware of\n    excisions through time <= t.\n\n    Does not communicate with the transactor, so the future may be\n    available immediately.\n\n    The future can take arbitrarily long to complete.  Waiters\n    should specify a timeout.", const__25, "Used to coordinate with background schema changes. Returns a\n    future that will acquire a database value that is aware of\n    all schema changes through time <= t.\n\n    Does not communicate with the transactor, so the future may be\n    available immediately.\n\n    The future can take arbitrarily long to complete.  Waiters\n    should specify a timeout.", const__26, "Returns true if db has had a filter set with filter", const__27, "Request the release of resources associated with this connection.  \nMethod returns immediately, resources will be released \nasynchronously. This method should only be called when the entire \nprocess is no longer interested in the connection. Note\nthat Datomic connections do not adhere to an acquire/use/release \npattern.  They are thread-safe, cached, and long lived.  Many \nprocesses (e.g. application servers) will never call release.", const__28, "Returns a fabricated entity id in the supplied partition whose\n\t T component is at or after the supplied t. Entity ids sort by partition,\n\t then T component, such T components interleaving with transaction numbers.\n\t Thus this function can be used to fabricate a time-based entity id component for use\n\t in e.g. seek-datoms.", const__29, "Returns the t one beyond the highest reachable via this db value.", const__30, "Returns information about the attribute with the given id or ident.\nSupports ILookup interface for key-based access. Supported keys are:\n\n:id, :ident, :cardinality, :value-type, :unique, :indexed, :has-avet,\n:no-history, :is-component, :fulltext\n", const__31, "Queries for database stats. Returns a map including at least:\n  :datoms  total count of datoms in the (history) database", const__32, "Used to coordinate with other peers.\n\n    When called with a t: returns a future that will acquire a\n    database value with basisT >= t. Does not communicate with the\n    transactor.\n\n    When called with no t: Returns a future that will acquire a\n    database value guaranteed to include all transactions that were\n    complete at the time sync was called.  Communicates with the\n    transactor.\n\n    db is the preferred way to get a database value, as it does not\n    need to wait nor block. Only use sync when coordination is\n    required, and prefer the two-argument version when you have a\n    basis t.\n\n    The future returned by sync can take arbitrarily long to\n    complete.  Waiters should use deref forms that specify a timeout.", const__33, "Register a completion listener for the future. The listener\n   will run once and only once, if and when the future's work is\n   complete. If the future has completed already, the listener will\n   run immediately.  Ordering of listeners is not guaranteed.", const__34, "Executes the query described by query-map\n\n   query-map form is {:query query\n                      :args args\n                      :timeout time-in-milliseconds\n                      :io-context qualified-keyword}\n\n   The query parameter is the same format as described in q.\n\n   The args parameter is the same format as inputs described in q.\n\n   The optional timeout is the number of milliseconds after which a\n   query may be stopped. Note: timeout is approximate, it is meant to\n   protect against long running queries, but is not guaranteed to stop\n   after precisely the duration specified.\n\n   You can get information about the I/O reads performed by a query\n   with io-stats. To request io-stats, add :io-context (a qualified\n   keyword) to the map you use to call query. Query will return a map\n   with\n\n   :ret                the result of the query\n   :io-stats           io-stats for the query\n\n   The io-stats map includes:\n\n   :io-context         the io-context passed in\n   :api                :tx-with\n   :api-ms             msec to perform the API call\n   :reads              breakout of reads by cache tier and index sort\n   :nested             breakout iff nested queries set :io-context\n\n   See https://docs.datomic.com/reference/io-stats.html.", const__35, "Gets the data queue associated with this connection, creating one\n   if necessary. At any point in time either zero or one queue is\n   associated with a connection. The returned queue may be consumed\n   from more than one thread. Note that the returned queue does not\n   block producers, and will consume memory until you consume the\n   elements from it. Reports will be added to the queue at some point\n   after the db has been updated. If this connection originated the\n   transaction, the transaction future will be notified first, before\n   a report is placed on the queue.\n\n   Reports are records with the following keys:\n\n     :db-before    value of database before the transaction\n     :db-after     value of database after the transaction\n     :tx-data      the transaction data in E/A/V/Tx form.", const__36, "Returns a list of database names. URI is a database URI as\n   described under the connect documentation, but with a '*' where the\n   database name would be. For instance: datomic:dev://{transactor-host}:{port}/*.\n   When using the map form, :db-name should be omitted.", const__37, "Connects to the specified database, returing a Connection.\n   URI syntax ({} indicate place holders to fill in, [] indicate optional):\n\n   DynamoDB using roles:\n   datomic:ddb://{aws-region}/{dynamodb-table}/{db-name}\n\n   DynamoDB using keys (use roles if possible):\n   datomic:ddb://{aws-region}/{dynamodb-table}/{db-name}?aws_access_key_id={XXX}&aws_secret_key={YYY}\n\n   DynamoDB Local\n   datomic:ddb-local://{endpoint:port}/{dynamodb-table}/{db-name}?aws_access_key_id={XXX}&aws_secret_key={YYY}\n\n   Couchbase:\n   datomic:couchbase://{host}/{bucket}/{dbname}[?password={xxx}]\n\n   SQL:\n   datomic:sql://{db-name}?{jdbc-url}\n\n   Infinispan:\n   datomic:inf://{cluster-member-host}:{port}/{db-name}\n\n   Cassandra:\n   datomic:cass://{cluster-member-host}[:{port}]/{keyspace}.{table}/{db-name}[?user={user}&password={pwd}][&ssl=true]\n\n   Cassandra3:\n   datomic:cass3://{cluster-member-host}[:{port}]/{keyspace}.{table}/{db-name}[?user={user}&password={pwd}][&ssl=true][&local-datacenter=datacenter1]\n\n   Dev Appliance: \n   datomic:dev://{transactor-host}:{port}/{db-name}[?password={password}]\n\n   Free transactor integrated storage:\n   datomic:free://{transactor-host}:{port}/{db-name}[?password={password}]\n\n   In-process Memory:\n   datomic:mem://{db-name}\n\n   Note that URIs must be percent encoded and db-name cannot contain the following characters: / \" * : = ?\n\n   The dev and free protocols use additional ports to communicate with\n   storage.  By default, this ports is one higher than the specified\n   transactor port. You can override the default by specifying h2-port\n   in the query string, e.g.\n\n     datomic:dev://localhost:4334/mydb?h2-port=6000\n\n   The sql protocol also supports a map format instead of the URI\n   string. This is to enable specifying objects that can't be\n   embedded in URI strings, like DataSources. The format for the\n   SQL map is:\n\n     {:protocol :sql                  ;; keyword or string\n      :db-name \"myDb\"               ;; keyword or string\n\n      :data-source aDataSourceObject\n       ;; OR\n      :factory aCallableReturningConnection}\n\n   Note only one of data-source or factory should be supplied.\n\n   The cass protocol also supports a map format instead of the URI\n   string. This is to enable specifying objects that can't be embedded\n   in URI strings. The format for the Cassandra map is:\n\n     {:protocol :cass                 ;; keyword or string\n      :db-name \"myDb\"               ;; keyword or string\n      :table \"myKeyspace.myTable\"\n      :cluster aClusterObject}\n\n   Note that aClusterObject must be an instance of type\n   com.datastax.driver.core.Cluster.\n\n   The cass3 protocol also supports a map format instead of the URI\n   string. This is to enable specifying objects that can't be embedded\n   in URI strings. The format for the Cassandra map is:\n\n     {:protocol :cass                 ;; keyword or string\n      :db-name \"myDb\"               ;; keyword or string\n      :table \"myKeyspace.myTable\"\n      :session aSessionObject}\n\n   Note that aSessionObject must be an instance of type\n   com.datastax.oss.driver.api.core.cql.SyncCqlSession.\n\n   Datomic connections do not adhere to an acquire/use/release\n   pattern. They are thread-safe and long lived. Connections are\n   cached such that calling datomic.api/connect multiple times with\n   the same database value will return the same connection object.", const__38, "d/with is a pure function that takes a database value and a\n         set of information (tx-data; held to be true at a point in time),\n         and returns a new database value that includes, via accretion,\n         that new information.\n\n         The tx-data argument is semantically an unordered set of information.\n         Syntactically it is a list that can include primitive assertions,\n         entity maps, and transaction functions.\n         See https://docs.datomic.com/transactions/transaction-data-reference.html.\n\n         If the tx-data is valid, returns a map containing the following keys:\n          :db-before  database value before the transaction\n          :db-after   database value after the transaction\n          :tx-data    collection of Datoms produced by the transaction\n          :tempids    argument to resolve-tempids\n\n         See d/datoms for a description of :tx-data.\n\n         Optional args:\n          :io-context a qualified keyword\n          When provided, the return map will have an additional :io-stats key describing\n          information about the I/O reads performed by the call to d/with.\n          For more detail, see https://docs.datomic.com/reference/io-stats.html.\n\n          :return-hints true\n          When provided, the return map may also include an additional key :hints,\n          an opaque value that can be passed as an option to d/transact or d/transact-async\n          to optimize performance.\n          For more detail, see https://docs.datomic.com/reference/hints.html.", const__39, "Returns the since point, or nil if none", const__40, "Shutdown all peer resources.  This method should be called as\npart of clean shutdown of a JVM process.  Will release all Connections,\nand, if shutdown-clojure is true, will release Clojure resources.\nPrograms written in Clojure can set shutdown-clojure to false if they\nmanage Clojure resources (e.g. agents) outside of Datomic; programs\nwritten in other JVM languages should typically set shutdown-clojure\nto true.", const__41, "Deletes the database specified by uri. Returns true if the\n   delete occurred. See connect for a description of the URI\n   syntax.", const__42, "Returns the implicit partition (an entity id) corresponding to the given id, where 0<=id<524288.", const__43, "Renames the database specified by uri to new-name. Returns\n   true if rename succeeded. See connect for a description of the\n   URI syntax.", const__44, "Returns the t of the most recent transaction reachable via this db value.", const__45, "Returns the keyword associated with an id, or the key itself if passed.", const__46, "Raw access to the index data, by index. The index must be supplied,\n   and, optionally, one or more leading components of the index can be\n   supplied to narrow the result.\n\n\t :eavt and :aevt indexes will contain all datoms\n\t :avet contains datoms for attributes where :db/index = true.\n\t :vaet contains datoms for attributes of :db.type/ref\n         :vaet is the reverse index\n\n   Returns a java.lang.Iterable of datoms. Datoms are associative and indexed:\n\n   Key     Index        Value\n   --------------------------\n   :e      0            entity id\n   :a      1            attribute id\n   :v      2            value\n   :tx     3            transaction id\n   :added  4            boolean add/retract", const__47, "Retrieves a value of the log for use in tx-range or query.", const__48, "get the time part of a squuid (a UUID created by squuid), in\nthe format of System.currentTimeMillis", const__49, "Resolve a tempid to the actual id assigned in a database. The\ntempids object must come from the :tempids member returned through\ntransact or transact-async.", const__50, "Returns a range of transactions in log, starting at start,\nor from beginning if start is nil, and ending before end, or through\nend of log if end is nil. start and end can be can be a transaction\nnumber, transaction ID, Date or nil.\n\nEach transaction is a map with the following keys:\n :t - the T point of the transaction\n :data -  a Collection of the Datoms asserted/retracted by the transaction", const__51, "Return the t value associated with a transaction id.", const__52, "Returns an Iterable range of datoms in index named by attrid,\nstarting at start, or from beginning if start is nil, and ending\nbefore end, or through end of attr index if end is nil.\n\nSee datoms for a description of the returned value.", const__53, "Generates a function object given a map with required keys\n\n  :lang  - clojure or java\n  :params - a list of parameter names used in the code\n  :code - a string or data containing the code of the body\n\n  and optional keys\n\n  :imports - a list to be spliced into (import ...)\n  :requires - a list to be spliced into (require ...)\n\n  Clojure code should consist of a single expression in which\n  the params will be in scope.\n\n  Returns a function object that implements IFn, and is a record with\n  keys :lang, :params, :code, :imports, and :requires.", const__54, "Walks an index, pulling entities via :e if :avet or :v if :aevt,\n        using the selector, returning a lazy seq on the results.\n\n        :index     :avet or :aevt\n        :selector  a pull selector (see 'pull')\n        :start     A vector in the same order as the index indicating\n                   the initial position. At least :a must be specified.\n                   Iteration is limited to datoms matching :a.\n        :reverse   optional, when true iterate the index in reverse\n                   order", const__55, "Returns the database value that is the basis for this entity", const__56, "Touches all of the attributes of the entity, including any component entities recursively.\n    Returns the entity.", const__57, "Returns the value of the database containing only datoms\nsatisfying the predicate. the predicate will be passed two arguments -\nthe unfiltered db and a Datom. Chained calls compose the predicate with\n'and'", const__58, "Like pull-many, but takes a single eid.", const__59, "Returns the id of the given implicit partition, where 0<=id<524288. Returns nil when arg not an implicit partition.", const__60, "Allow storage to reclaim garbage older than a certain age.", const__61, "Schedules a re-index of the database. The re-indexing happens\n   asynchronously. Returns true if re-index is scheduled."}));
        Object chunk_19642 = null;
        long count_19643 = 0L;
        long i_19644 = 0L;
        while (true) {
            Object temp__5457__auto__19654;
            if (i_19644 < count_19643) {
                Object vec__19646 = ((Indexed)chunk_19642).nth(RT.intCast((long)i_19644));
                Object v = RT.nth((Object)vec__19646, (int)RT.intCast((long)0L), null);
                Object object = vec__19646;
                vec__19646 = null;
                Object doc = RT.nth((Object)object, (int)RT.intCast((long)1L), null);
                Object object2 = v;
                v = null;
                Object object3 = doc;
                doc = null;
                ((IFn)const__66.getRawRoot()).invoke(object2, const__67.getRawRoot(), (Object)const__68, object3);
                Object object4 = seq_19641;
                seq_19641 = null;
                Object object5 = chunk_19642;
                chunk_19642 = null;
                ++i_19644;
                chunk_19642 = object5;
                seq_19641 = object4;
                continue;
            }
            Object object = seq_19641;
            seq_19641 = null;
            Object object6 = temp__5457__auto__19654 = ((IFn)const__0.getRawRoot()).invoke(object);
            if (object6 == null || object6 == Boolean.FALSE) break;
            Object object7 = temp__5457__auto__19654;
            temp__5457__auto__19654 = null;
            Object seq_196412 = object7;
            Object object8 = ((IFn)const__70.getRawRoot()).invoke(seq_196412);
            if (object8 != null && object8 != Boolean.FALSE) {
                Object c__5719__auto__19653 = ((IFn)const__71.getRawRoot()).invoke(seq_196412);
                Object object9 = seq_196412;
                seq_196412 = null;
                Object object10 = c__5719__auto__19653;
                Object object11 = c__5719__auto__19653;
                c__5719__auto__19653 = null;
                i_19644 = RT.intCast((long)0L);
                count_19643 = RT.intCast((int)RT.count((Object)object11));
                chunk_19642 = object10;
                seq_19641 = ((IFn)const__72.getRawRoot()).invoke(object9);
                continue;
            }
            Object vec__19649 = ((IFn)const__75.getRawRoot()).invoke(seq_196412);
            Object v = RT.nth((Object)vec__19649, (int)RT.intCast((long)0L), null);
            Object object12 = vec__19649;
            vec__19649 = null;
            Object doc = RT.nth((Object)object12, (int)RT.intCast((long)1L), null);
            Object object13 = v;
            v = null;
            Object object14 = doc;
            doc = null;
            ((IFn)const__66.getRawRoot()).invoke(object13, const__67.getRawRoot(), (Object)const__68, object14);
            Object object15 = seq_196412;
            seq_196412 = null;
            i_19644 = 0L;
            count_19643 = 0L;
            chunk_19642 = null;
            seq_19641 = ((IFn)const__76.getRawRoot()).invoke(object15);
        }
        return null;
    }

    public Object invoke() {
        return api$fn__19645.invokeStatic();
    }
}

