# Goal 8 Continuation Prompt

```text
Continue Goal 8, lifecycle and operational completion, using
/home/jake/Developer/atomic/goal-8/0-plan.md and
/home/jake/Developer/atomic/goal-8/0-loop.md.

Treat datomic_pro_docs as semantic/operational authority, completed Goal 1–7
behavior as the executable system boundary, and 1.0.7705 backup, integrity,
monitor, garbage, excise, indexing and lifecycle code as the default structural
blueprint. Use Rust and PostgreSQL only; use PostgreSQL-native backup/PITR and
coordination strengths directly and build no generic backend/exporter/service
framework.

Reconcile the plan with artifacts and real PostgreSQL evidence, then execute
the first unfinished stage. Deliver bounded integrity/metrics/capacity APIs,
external consistent backup plus selectable restore/deep verification, safe
derived-value GC, checksum-guarded evolution, and privileged audited excision
that removes raw authoritative and derived copies while retaining a
non-sensitive record. Destructive work requires a separately verified backup
acknowledgement, exact target checks, atomic rollback evidence, and restart/raw
scan verification. Keep runbooks concise and executable.

Continue through the integrated Goal 0 audit: real PostgreSQL, external backup
destination/restore target, concurrency, restart, corruption, failure, privacy,
formatting and strict lint. Reopen earlier ownership for any gap. Finish only
when the PostgreSQL-only system is demonstrably operable and the original Goal
0 success condition holds; otherwise report a concrete blocker plainly.
```
