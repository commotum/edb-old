# Goal 17 Execution Loop

1. Sync `0-plan.md` with Goal 9 C32--C35, completed Goal 16 contracts, current
   public APIs, and actual tests. Keep source semantics distinct from native
   safety/performance requirements.
2. Select the first unfinished stage. Trace the corresponding docs and
   recovered 1.0.7705 connection, `Db`, cursor, notification, temporal, or
   synchronization path before changing behavior.
3. Add one decisive source witness, then implement the smallest coherent
   native slice. Reuse the concrete tiered value, persistent/recent trees,
   shared cache/pins, fenced client, and PostgreSQL publication coordinates.
4. Verify semantics against the eager oracle where useful, and verify laziness
   with construction/first-yield node reads, retained bytes, materialization
   counters, and old-value behavior. Run risky sync/report/restart boundaries
   on PostgreSQL and prove the work actually executed.
5. Record consequential findings, ambiguity decisions, deviations, and
   truthful stage status in `0-plan.md`. Revise later stages when evidence
   changes their best boundary.
6. Continue through the stages. Do not count facade/type churn, collecting
   adapters, self-skipping integrations, or a scaffold as completion.
7. Before closure, audit every ordinary connect/db/sync/report/raw/time path
   for eager recovery, compatibility cells, whole-index collection, forward-
   then-reverse, duplicate reporting, hidden queue retention, and lock-held
   waits. Run format, all-target check, warning-denying Clippy, pure source
   witnesses, and the focused non-skipping PostgreSQL matrix.
8. When the exit condition is established, fold evidence into Goals 9 and 0,
   commit the coherent checkpoint, and return immediately to the parent loop
   for Goal 18. If blocked, record the concrete red witness and required
   authority without narrowing the objective.

Prefer direct working API slices over frameworks. Do not copy JVM connection
registries or collection wrappers when `Arc`, Rust iterators, and the existing
PostgreSQL-concrete components express the same semantics more clearly.
