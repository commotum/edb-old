# Goal 15 Execution Loop

1. Sync `0-plan.md` with the repository, Goal 9's evidence ledger, current
   operations code, migrations, tests, and explicit live-PostgreSQL results.
2. Select the first unfinished stage. Before changing behavior, trace its
   observable contract in `datomic_pro_docs` and the matching 1.0.7705 SQL,
   backup, garbage, excision, index, or lifecycle path.
3. Write or expose the smallest decisive regression for the real defect, then
   implement the direct PostgreSQL/Rust correction. Preserve the authoritative
   log/derived-root direction and keep Goal 16 writer work out of this child.
4. Exercise risky boundaries proportionally: separate roles, version skew,
   TLS, concurrent publication, corruption isolation, backup crash/retry,
   exact restore, pins/grace, excision closure, restart, and peer adoption.
5. Treat a test that returns early without `ATOMIC_POSTGRES_URL` as skipped,
   never as PostgreSQL evidence. Record server/version and actual executed test
   counts for completion claims.
6. Fold material source findings, Rust/PostgreSQL deviations, measurements,
   and truthful stage status into `0-plan.md`, then continue to the next stage.
7. Before closure, run the focused operational matrix plus format, Clippy, and
   relevant pure/integration regressions. Apply the process-honesty audit: a
   scaffold, document, mock, or self-skipping green test is not completion.
8. When the exit condition is established, update Goal 9 and Goal 0 and return
   to the parent loop for Goal 16. If interrupted, leave the exact first red
   witness or next command in the plan.
