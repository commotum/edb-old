```text
Continue Goal 15, PostgreSQL lifecycle and operational safety for the native
Rust Datomic-inspired database, using
/home/jake/Developer/atomic/goal-15/0-plan.md and
/home/jake/Developer/atomic/goal-15/0-loop.md.

Treat datomic_pro_docs as semantic authority, Goals 10–14 as the established
kernel/peer boundary, Goal 9's evidence ledger as the corrective map, and
1.0.7705 as the default blueprint for SQL lifecycle, immutable root
publication, backup, garbage, excision, and synchronization. Use Rust and
PostgreSQL directly. Keep the log authoritative and roots derived; do not add
backend portability or pull Goal 16's writer conversion into this work.

Sync the plan with current code and real PostgreSQL evidence, advance the first
unfinished stage, and keep working through explicit migration/runtime
authority, TLS/version checks, coherent scoped inspection, crash-safe
differential backup and exact restore, safe pin/grace-aware reclamation,
source-faithful auditable excision, and operational closure. Verify risky
concurrency, corruption, retry, restart, and privacy boundaries with tests that
actually execute PostgreSQL. Fold material findings and status into the plan
so work remains resumable. Continue until Goal 15's exit condition is genuinely
met and its evidence is folded into Goal 9 and Goal 0; report a concrete
blocker or uncertainty plainly rather than narrowing the objective.
```
