# Goal 10 Working Loop

1. Reconcile this plan with `goal-9/EVIDENCE_LEDGER.md`, current code, and test
   output; select the first unfinished stage.
2. Trace the exact documentation and recovered function/type named by the
   relevant ledger entry before changing representation or behavior.
3. Add or tighten the smallest executable witness that exposes the current gap.
4. Implement the coherent kernel change, following identity and schema
   consequences through every caller rather than adding compatibility aliases
   that leave two meanings alive.
5. Run focused tests immediately, then broader pure and PostgreSQL gates as the
   risk expands. Make missing PostgreSQL prerequisites explicit.
6. Record material decisions and truthful status here. When the exit condition
   is met, update `goal-9/0-plan.md` and reopened `goal-0/0-plan.md`, then return
   to the Goal 9 loop and scaffold/execute Goal 11.
7. If interrupted, leave the failing/passing witness and next implementation
   action explicit. Do not stop at documentation or a partially propagated ID
   representation.
