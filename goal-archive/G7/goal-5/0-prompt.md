```text
Complete Atomic's Rust-owned retention, incremental reclamation, catalog lifecycle
and excision using /home/jake/Developer/atomic/goal-5/0-plan.md and 0-loop.md.
This is Stage 5 of /home/jake/Developer/atomic/goal-0/0-plan.md, not another parent.
Sync actual code/tests, resume the first unfinished internal stage, and execute
with best judgment. Preserve immutable values, exact retry/lifecycle semantics,
and protected publication over opaque PostgreSQL storage. No engine-policy SQL,
legacy fallback, live-data GC scans, unrelated resets or reference-corpus deletion.
Use permanent regressions, actual PostgreSQL/application checks and measured full
costs. Fold results and continuation into both plans. Finish only when this stage
holds, report gaps plainly, then return to the parent's remaining stages.
```
