# Goal 1 — Continuation prompt

```text
Execute Atomic's storage-foundation child using
/home/jake/Developer/atomic/goal-1/0-plan.md and
/home/jake/Developer/atomic/goal-1/0-loop.md.

This is Stage 1 of /home/jake/Developer/atomic/goal-0/0-plan.md, not a new parent.
Build and verify the real PostgreSQL opaque immutable-object/revisioned-reference
store, authenticated Rust root descriptors and coherent publication, fencing and
reader/build protection primitives. PostgreSQL supplies generic atomic storage;
Rust owns database policy. Use local datomic_pro_docs as semantic authority and
1.0.7705 as architectural evidence, translating idiomatically rather than literally.

Reconcile existing work, execute unfinished internal stages, preserve working code
and unrelated databases, and use fresh isolated PostgreSQL tests with measured
complete-path costs. No compatibility backend, old-format readers, JVM parity or
recursive scaffolds. Record results/gaps in child and parent. After this child's
completion signal, return to Goal 0 and continue the full cutover; this child alone
is not the user's finish line.
```
