```text
Continue Goal 17, the native connection and immutable database API for the
Rust/PostgreSQL Datomic-inspired database, using
/home/jake/Developer/atomic/goal-17/0-plan.md and
/home/jake/Developer/atomic/goal-17/0-loop.md.

Treat datomic_pro_docs as semantic authority, Goal 9 C32--C35 and completed
Goal 16 as executable boundaries, and recovered 1.0.7705 Connection, Db,
notification, raw-index, temporal, and synchronization paths as the default
blueprint. Preserve one cloneable shared connection, exact immutable old
values, complete opt-in reports, lazy forward/reverse native traversal, T/Tx/
instant time points, entid-at, and distinct transaction/index/schema/excision
progress. Use Rust and PostgreSQL directly; ordinary APIs must never hide eager
Database reconstruction, a collecting reverse implementation, or a missing-
root fallback.

Sync the plan with current code, advance the first unfinished stage with
source witnesses and working behavior, verify boundedness and risky report/
sync/restart boundaries on real PostgreSQL, and fold material decisions and
truthful status back into the plan. Keep Goal 18 transaction-value work, Goal
19 query-surface expansion, generic storage abstraction, JVM machinery, and
deployment ceremony out of scope. Continue until Goal 17's full exit condition
is established, then update the parent loop and advance; report a genuine
blocker or uncertainty plainly rather than narrowing the objective.
```
