# Goal 2 Continuation Prompt

```text
Continue Goal 2, the production single-process transactional kernel, using /home/jake/Developer/atomic/goal-2/0-plan.md and /home/jake/Developer/atomic/goal-2/0-loop.md.

Treat datomic_pro_docs as semantic authority, /home/jake/Developer/atomic/goal-1/SEMANTICS.md and its tests as the acceptance boundary, and 1.0.7705 as the default structural and algorithmic blueprint. Trace recovered names, types, representations, and control flow, preserve them through the closest idiomatic Rust design where sound, and record significant justified deviations. Keep transactions declarative, unordered, pure over immutable db-before, atomic, and deterministic.

Build real Rust kernel behavior and verify it against the reference model; do not substitute planning or type scaffolding for working increments. Keep PostgreSQL, generic storage abstraction, networking, distributed services, full Datalog/pull engines, and persisted WASM/DSL deployment out of this milestone. Sync the plan to current evidence, advance the first unfinished stage, run proportionate checks, and fold material results and truthful status back into the plan so the goal remains resumable. Continue until the kernel exit condition is achieved, reporting genuine blockers or uncertainty plainly.
```
