# Goal 1 Continuation Prompt

```text
Continue the staged validation and hardening of the recovered Datomic peer in /home/jake/Developer/atomic/datomic-rev. The objective is to produce and verify a reproducible source-built artifact, validate SQL/backup/recovery behavior, add concurrency and fault-injection coverage, repair compiler warnings only when original bytecode uniquely proves the correction, and only then improve readability without changing behavior.

Treat /home/jake/Developer/atomic/goal-1/0-plan.md as the authoritative strategy and /home/jake/Developer/atomic/goal-1/0-loop.md as the execution rhythm. Sync the plan with the actual repository and evidence, then work through the first unfinished stage using best judgment. Do not allow original peer/core2 AOT classes to satisfy source-built validation, do not guess or suppress warning fixes, and use only disposable authorized environments for backend or fault testing.

Fold material results, decisions, evidence, limitations, stage status, and the clearest next action back into the plan so the goal remains resumable across sessions. Continue until every stage's observable completion signal is satisfied; report genuine blockers or uncertainty plainly rather than claiming completion.
```
