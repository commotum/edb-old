# Goal 2 execution loop

1. Read this plan and Goal0's plan/loop. Reconcile real code, tests and earlier
   repairs; keep R6/R12 in scope and Goal2 the only active child.
2. Select the first unfinished step. Establish a permanent safe regression, repair
   the shared path, or verify its actual application behavior—whichever directly
   advances the outcome. Parallelize only bounded work inside this child.
3. Use docs/source evidence for component and sharing semantics. Preserve immutable
   values, durable meaning, canonical reports and existing resource admission;
   choose explicit iteration/ownership rather than arbitrary depth exclusions.
4. Validate small-stack, shared-owner, failure and real PG/restart checks. Count
   complete traversal/drop work; state fixture and measurement limits honestly.
5. Fold material results, decisions and status into this plan and parent Stage2.
   Continue until the combined completion signal; re-open earlier owning children
   for integrated gaps rather than excluding them or creating a corrective parent.
6. At a session boundary record last verified state, next concrete action and any
   fixture/authority blocker. Keep continuation concise and executable.
7. On completion return to `/home/jake/Developer/atomic/goal-0/0-loop.md` and
   scaffold/resume and execute its next unfinished stage. Goal2 is not the parent's
   finish line; the original repaired-product objective remains the goal.
