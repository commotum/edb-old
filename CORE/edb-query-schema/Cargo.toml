[package]
name = "edb-query-schema"
version = "0.1.0"
edition = "2021"

[dependencies]
rusqlite = { version = "0.30", features = ["bundled"] }
edb-encoding = { path = "../edb-encoding" }
edb-schema = { path = "../edb-schema" }
