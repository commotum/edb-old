[package]
name = "edb-tx"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["std", "serde"] }
thiserror = "1"
edb-encoding = { path = "../edb-encoding" }
edb-schema = { path = "../edb-schema" }
